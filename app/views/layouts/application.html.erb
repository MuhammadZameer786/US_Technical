<!DOCTYPE html>
<html>
<head>
  <title>Union Swiss - Order System</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    :root {
      --us-navy: #2c3e50;
      --us-light: #f8f9fa;
    }

    body {
      background-color: var(--us-light);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Change the standard Bootstrap 'bg-primary' to Union Swiss Navy */
    .bg-primary { background-color: var(--us-navy) !important; }
    .btn-primary { background-color: var(--us-navy); border-color: var(--us-navy); }
    
    /* Make the cards look modern */
    .card { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); border-radius: 0.5rem; }
    
    /* Add a nice hover to tables */
    .table-hover tbody tr:hover { background-color: rgba(44, 62, 80, 0.05); }

    main { flex: 1; }
  </style>
</head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <%= link_to "Union Swiss", root_path, class: "navbar-brand" %>
        
        <% if logged_in? %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>
          
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
              <% if current_user.admin? %>
                <li class="nav-item">
                  <%= link_to "Dashboard", admin_dashboard_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Distributors", admin_distributors_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Products", admin_products_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Users", admin_users_path, class: "nav-link" %>
                </li>
              <% elsif current_user.distributor? %>
                <li class="nav-item">
                  <%= link_to "Dashboard", distributors_dashboard_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "My Orders", distributors_orders_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "New Order", new_distributors_order_path, class: "nav-link" %>
                </li>
              <% end %>
            </ul>
            
            <div class="d-flex align-items-center text-white">
              <span class="me-3"><%= current_user.email %></span>
              <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
            </div>
          </div>
        <% end %>
      </div>
    </nav>

    <main class="py-4">
      <div class="container">
        <% if flash[:notice] %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= flash[:notice] %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
        <% end %>

        <% if flash[:alert] %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= flash[:alert] %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </main>

    <footer class="bg-light py-3 mt-5">
      <div class="container text-center text-muted">
        <small>&copy; <%= Time.current.year %> Union Swiss. All rights reserved.</small>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
